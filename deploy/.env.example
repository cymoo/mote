# Required: application password (set a strong random string)
MOTE_PASSWORD=change-me

# Required: domain name (used by Caddy to obtain a TLS certificate)
DOMAIN=example.com

# Optional overrides (defaults shown)
# POSTS_PER_PAGE=20
# LOG_REQUESTS=false
# HTTP_MAX_BODY_SIZE=10M
